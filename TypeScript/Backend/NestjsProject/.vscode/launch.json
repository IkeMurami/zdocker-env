{
    "version": "0.2.0",
    "configurations": [
        {
            "address": "localhost",
            "localRoot": "${workspaceFolder}/app",
            "name": "Docker: Attach to Node",
            "port": 9229,
            "remoteRoot": "/app",
            "request": "attach",
            "type": "node"
        }
    ]
}